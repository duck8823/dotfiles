---
name: codex-handoff
description: Claude から Codex へ実装・調査タスクを引き継ぐときに使う。目的、完了条件、非対象、制約、テスト範囲、出力形式を固定し、引き継ぎ品質のぶれを防ぐ。
---

# Codex Handoff

## 引き継ぎテンプレート（入力）
- 目的
- 完了条件（受け入れ条件）
- 非対象（やらないこと）
- 制約（ブランチ、依存、期限、禁止操作）
- テスト範囲（必須コマンド）
- Structure-Behavior risk（Low / Medium / High）
- Medium 以上の Design Note（要求・概念モデル・責務表・境界/IF・振る舞いテスト）
- 期待出力（PR / 差分要約 / 調査メモ）

## 手順
1. 曖昧語を具体化する（「適切に」などを禁止）。
2. 完了条件を検証可能な文に直す。
3. 非対象を明記してスコープを固定する。
4. 失敗時の扱い（リトライ条件、エスカレーション条件）を先に決める。
5. Medium / High risk では、手続き化・責務漏れ・境界/IF・振る舞いテスト不足のレビュー観点を入れる。
6. 最終依頼文を1本化する。

## 出力フォーマット
- 実施内容サマリ
- 変更ファイル一覧
- テスト結果
- Structure-Behavior レビュー結果（該当する場合）
- 未解決事項
- 次アクション

## 禁止
- 目的と完了条件がない依頼を渡す
- テスト範囲なしで実装を渡す
- スコープ未固定のまま着手する
